# <GAME> — blockers

<!-- A slice that can't be finished byte-exact is REVERTED FULLY and logged
     here — never worked around. A returning agent scans this file before
     picking targets: blockers often dissolve once a lower layer is recovered
     (pitfall #20). Move resolved entries to the bottom with the resolution. -->

## Open

### YYYY-MM-DD — <one-line symptom>

- **Where:** <CS:IP / routine / subsystem>
- **Evidence:** <trace/snapshot/divergence-artifact paths — enough to reproduce>
- **Attempts:** <what was tried (~2 focused attempts before logging), what each showed>
- **Hypothesis:** <best current theory, marked as theory>
- **Likely unblocks when:** <e.g. "the collision layer below is recovered">

## Resolved

### YYYY-MM-DD — <symptom> → resolved YYYY-MM-DD

- **Resolution:** <what actually unblocked it — often not the hypothesis>
